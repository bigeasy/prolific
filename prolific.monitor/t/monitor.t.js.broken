require('proof')(1, require('cadence')(prove))

function prove (async, assert) {
    var monitor = require('../monitor.bin')

    var io
    async(function () {
        io = monitor({}, [ '--url', 'test://127.0.0.1' ], {}, async())
    }, function (code) {
        assert(code, 0, 'code')
    })
}
