module.exports = function (destructible, configuration, nextProcessor, callback) {
    callback(null, {
        process: function (entry) {
            nextProcessor.process(entry)
        }
    })
}

module.exports.isProlificProcessor = true
